# 稳定窗口缓存交付追溯与发布审计

本文件把设计规格第 6–18 节映射到独立包的实现、精确测试和验收命令。状态仅表示 2026-08-28 本地审计可证明的范围；310P 运行时正确性与性能必须在目标机实测，不能由 Fake Pipeline 推断。

## 规格追溯

| 规格 | 需求与实现符号 | 精确测试 | 验收命令 | 状态 |
| --- | --- | --- | --- | --- |
| 6.1、9、12 | `validate_session_scope` 在 namespace、`build_request`、`release_session` 构造状态键前统一要求 exact non-empty/non-whitespace `str` 和 exact non-negative `int`（拒绝 `bool`）；非空 Session ID 的前后空白保留 | `test_session_namespace_rejects_invalid_exact_session_ids`; `test_session_namespace_rejects_invalid_exact_epochs`; `test_invalid_epoch_cannot_read_or_release_epoch_one_state`; `test_invalid_session_id_cannot_create_or_release_session_state` | `python -m pytest tests/unit/test_identity.py tests/unit/test_request_adapter.py -vv` | 已实现 |
| 6.1 | Adapter 接收单条累计 mono 16 kHz、C-contiguous `float32` PCM；配置固定窗口、模型和上限 | `test_builds_direct_vllm_prompt_across_growing_windows`; `test_invalid_pcm_layouts_are_rejected`; `test_non_target_sample_rate_is_rejected` | `python -m pytest tests/unit/test_request_adapter.py tests/unit/test_windowing.py -vv` | 已实现 |
| 6.2、12 | `build_request` 产生原生 PromptType；正常、warmup、pressure、final-retry 和专用 recreate 流在保留 exact-final retry 所需状态后于 `finally` 释放 CPU 元数据 | `test_builds_direct_vllm_prompt_across_growing_windows`; `test_normal_benchmark_releases_adapter_metadata_after_final_request`; `test_validation_retains_all_checkpoints_and_retries_the_exact_final_request` | `python -m pytest tests/unit/test_request_adapter.py tests/unit/test_benchmark_manifest.py -vv` | 已实现 |
| 7 | `split_audio_windows` 以视图分割 sealed/open/final 窗口，不重采样 | `test_four_second_windows_for_six_seconds`; `test_supported_window_sizes_partition_eight_seconds_without_a_tail`; `test_final_tail_is_sealed` | `python -m pytest tests/unit/test_windowing.py -vv` | 已实现 |
| 8 | 一个外层音频区间、每窗口一个 anchor；多项顺序与 MRoPE 连续性由固定版本契约锁定 | `test_replaces_one_placeholder_with_three_anchors`; `test_qwen3_asr_preserves_audio_embedding_order_and_mrope_continuity` | `python -m pytest tests/unit/test_prompt_builder.py -vv`; `python -m pytest -m contract tests/contract/test_vllm_023_contract.py -vv` | 已实现 |
| 9、10 | 规范 PCM、位置、指纹、schema 进入 window ID；Session/epoch 进入 namespace；Adapter 不持有 embedding；Fake cache 覆盖 sealed hit/open miss/LRU/历史 PCM 失效 | `test_each_window_identity_dimension_invalidates`; `test_four_second_stream_has_exact_reuse_trace_and_output_equivalence`; `test_historical_sealed_pcm_change_invalidates_affected_encoder_and_kv_tail` | `python -m pytest tests/unit/test_identity.py tests/integration/test_fake_cache_equivalence.py -vv` | 已实现 |
| 11 | `floor_reusable_prefix_tokens` 按选择的 32/128 向下取整；完整块的 key 排除变化的不完整尾部；上游契约锁定父 hash、token、MM identifier/offset 与 salt | `test_prefix_cache_hashes_only_complete_configured_blocks`; `test_incomplete_tail_uuid_change_preserves_complete_block_hit_and_output`; `test_block_hash_uses_multimodal_position_parent_and_first_block_salt` | `python -m pytest tests/unit/test_metrics.py tests/integration/test_fake_cache_equivalence.py -vv`; `python -m pytest -m contract tests/contract -vv` | 已实现 |
| 11、14、17.5 | `--hash-block-size {32,128}` 默认 32，传播到所有 Engine/matrix/NPU 路径并进入每请求结果、汇总、等价键和复现命令；128 只在 32 初始化/完整正确性失败后执行完整重跑 | `test_cli_accepts_only_audited_hash_block_sizes`; `test_selected_hash_block_size_reaches_reuse_engine_configuration`; `test_matrix_api_rejects_unaudited_hash_block_sizes`; `test_hash_block_size_is_part_of_equivalence_identity_and_summary` | `python benchmarks/benchmark_310p.py --model /models/Qwen3-ASR-1.7B --manifest /data/asr-validation.jsonl --window-seconds 2 4 8 --concurrency 1 4 8 --hash-block-size 32 --warmup-iterations 3 --iterations 20 --output /secure-validation/hash32-matrix.jsonl`; 128 完整命令见 `docs/310p-validation.md` | 本地接口已实现；目标机待验收 |
| 13 | 公共领域错误包括 `InvalidSessionId`、`InvalidUtteranceEpoch` 和 `InvalidEngineConfiguration`；非法 scope 在读写/释放前失败，配置失败前不改写 | 包根导入由 `test_request_adapter.py`/`test_engine_config.py` 的真实 imports 覆盖；`test_invalid_epoch_cannot_read_or_release_epoch_one_state`; `test_invalid_session_id_cannot_create_or_release_session_state`; `test_engine_args_config_factory_must_be_callable` | `python -m pytest tests/unit/test_identity.py tests/unit/test_request_adapter.py tests/unit/test_engine_config.py -vv` | 已实现 |
| 14 | 精确要求 vLLM/vLLM-Ascend 0.23.0；`prepare_vllm_config` 显式要求 callable `create_engine_config` 并设置哈希粒度 | `test_exact_supported_runtime_versions_pass`; `test_prepares_one_config_without_changing_other_engine_settings`; `test_engine_args_config_factory_must_be_callable` | `python -m pytest tests/unit/test_compatibility.py tests/unit/test_engine_config.py -vv`; `git -C .upstream/vllm describe --tags --exact-match`; `git -C .upstream/vllm-ascend describe --tags --exact-match` | 已实现 |
| 15、17.2 | `vllm:mm_cache_queries/hits` 的固定版本来源是 renderer/MM processor cache；派生字段命名为 `processor_cache_queries/hits/misses`，真实 Encoder hit/miss 无来源时固定 `null`/warning | `test_mm_cache_prometheus_counters_originate_from_renderer_processor_cache`; `test_request_telemetry_derives_cache_and_prefill_values_without_fabrication`; `test_request_json_keeps_encoder_unknown_and_names_processor_cache_provenance` | `python -m pytest tests/unit/test_benchmark_manifest.py tests/unit/test_prometheus_delta.py -vv`; `python -m pytest -m contract tests/contract/test_vllm_023_contract.py -vv` | 已实现 |
| 15 | 进程全局 before/after 只有并发 1 可做 per-request delta；并发大于 1 时四个 delta 和 processor 派生字段均为 `null` 并警告重叠；每请求 prefix token 仍独立记录 | `test_overlapping_concurrent_requests_do_not_receive_process_global_deltas`; `test_concurrent_request_telemetry_refuses_overlapping_counter_attribution` | `python -m pytest tests/unit/test_prometheus_delta.py tests/unit/test_benchmark_manifest.py -vv` | 已实现 |
| 15、17.5 | `BenchmarkResult`/summary 输出 hash block、稳定音频 token、prefix/prefill、TTFT、尾字时延和 P50/P95；前端不可得的 Encoder hit、分段与模型峰值内存保持 `null`/warning | `test_hash_block_size_is_part_of_equivalence_identity_and_summary`; `test_request_telemetry_marks_missing_or_impossible_values_unavailable`; `test_validation_retains_all_checkpoints_and_retries_the_exact_final_request` | `python -m pytest tests/unit/test_benchmark_manifest.py tests/unit/test_metrics.py tests/unit/test_prometheus_delta.py -vv` | 已实现 |
| 17.1 | 根据生产环境裁决，Python 元数据限定 `>=3.12,<3.13`，Ruff/mypy 目标 3.12 | 包元数据与本地实际 3.12 runtime 门禁 | `python --version`; `python -m ruff check src tests benchmarks`; `python -m mypy src benchmarks`; `python -m pytest` | 已实现 |
| 17.3 | Fake Audio Tower/LLM 覆盖 6→8→10 命中轨迹、重试、隔离、淘汰及完整 PCM 敏感性 | `test_equal_moment_permutation_exposes_deliberately_stale_encoder_reuse`; `test_four_second_stream_has_exact_reuse_trace_and_output_equivalence`; `test_session_and_epoch_identity_do_not_share_encoder_or_prefix_entries` | `python -m pytest tests/integration/test_fake_cache_equivalence.py -vv` | 已实现 |
| 17.4 | reset 要求 `await reset_prefix_cache()` 成功，随后 reset Encoder、释放 Adapter 并重放同一 scope；首个请求必须观测 cached prefix 为 0。LRU 对同一 target 记录 warm retry、施加 distinct-salt 压力再重放；只有 token 降低才认证 `after_lru_pressure` | `test_validation_retains_all_checkpoints_and_retries_the_exact_final_request`; `test_failed_prefix_reset_stops_before_encoder_reset_or_replay`; `test_reset_replay_requires_exact_observable_zero_cached_tokens`; `test_non_evicting_lru_replay_cannot_be_certified_as_after_pressure` | `python -m pytest tests/unit/test_benchmark_manifest.py -vv`; `python -m pytest -m npu tests/integration/test_310p_equivalence.py -vv` | 本地编排已实现；目标机待验收 |
| 17.4 | 外部数值 sidecar 以严格、非敏感 schema 校验 embedding/logits 误差、dtype 与 provenance，不 monkey-patch 或伪造公开 API 未暴露张量 | `test_numeric_error_sidecar_requires_exact_finite_non_sensitive_schema`; `test_numeric_error_sidecar_cli_is_lazy_and_strict` | `python benchmarks/benchmark_310p.py --validate-numeric-sidecar <sidecar>` | 本地校验已实现；目标采集待验收 |
| 17.5 | 目标机 Audio Tower/LLM prefill 分段、真实 Encoder Cache 来源、外部峰值 NPU 内存、无 profiler 性能矩阵、32/128 fallback 和数值 sidecar 归档 | `test_310p_cache_reuse_matches_full_recompute_after_lifecycle_events` | `mkdir -p /secure-validation/profiled-310p /secure-validation/memory; npu-smi info -t memory -i 0 > /secure-validation/memory/before.txt; (msprof --application="python benchmarks/benchmark_310p.py --model /models/Qwen3-ASR-1.7B --manifest /data/asr-validation.jsonl --window-seconds 2 4 8 --concurrency 1 4 8 --hash-block-size 32 --warmup-iterations 3 --iterations 20 --output /secure-validation/profiled-310p/timeline-input.jsonl" --output=/secure-validation/profiled-310p/msprof --analysis=on) & BENCH_PID=$!; (while kill -0 "$BENCH_PID" 2>/dev/null; do npu-smi info -t memory -i 0 >> /secure-validation/memory/during.txt; sleep 1; done) & MONITOR_PID=$!; wait "$BENCH_PID"; wait "$MONITOR_PID"; npu-smi info -t memory -i 0 > /secure-validation/memory/after.txt; python benchmarks/benchmark_310p.py --validate-numeric-sidecar /secure-validation/numeric-error.json` | 目标机待验收 |
| 18 | 不修改上游、无运行时代码 monkey patch、无注册原生模型/Processor、无 checkpoint 变更；固定上游 tag 可显式证明 | `test_upstream_trees_have_no_tracked_staged_or_untracked_changes`; `test_direct_script_help_does_not_require_vllm_npu_or_installed_package` | `git -C .upstream/vllm describe --tags --exact-match`; `git -C .upstream/vllm-ascend describe --tags --exact-match`; `bash scripts/verify_upstream_clean.sh`; `rg -n "monkey.?patch|register_model|register_processor|sys\\.path" src benchmarks` | 已实现 |

## 本次审计证据

以下命令均在 2026-08-28（Asia/Shanghai）从 final fix wave 的最终工作树重新执行。命令输出不包含真实 PCM、业务 Session 标识或 benchmark 结果。

| 命令 | 摘要 | 状态 |
| --- | --- | --- |
| `python -m pytest` | Python 3.12.13；238 passed，25 deselected | 已实现 |
| `python -m pytest -m contract tests/contract -vv` | 24 passed | 已实现 |
| `python -m pytest -m npu tests/integration/test_310p_equivalence.py -vv -rs` | 1 skipped；明确原因为未设置 `QWEN3_ASR_310P_MODEL` 和 `QWEN3_ASR_310P_MANIFEST` | 目标机待验收 |
| `python -m ruff check src tests benchmarks` | All checks passed | 已实现 |
| `python -m mypy src benchmarks` | 12 source files 无问题 | 已实现 |
| `python --version`（实际 Python 3.12 runtime） | Python 3.12.13；项目边界 `>=3.12,<3.13`，Ruff/mypy 目标 py312 | 已实现 |
| `python -m build --no-isolation --outdir <唯一临时目录>` | 成功生成 sdist 和 `py3-none-any` wheel；保留已知的缺少 README 发布警告 | 已实现 |
| `git -C .upstream/vllm describe --tags --exact-match`; `git -C .upstream/vllm-ascend describe --tags --exact-match` | 两者均为 `v0.23.0` | 已实现 |
| `bash scripts/verify_upstream_clean.sh` | 两个上游树均无 tracked/staged/untracked 变更 | 已实现 |
| 包根导入、侵入式/占位搜索、traceability validator、git diff/status | 包根不加载 vLLM/torch；PromptType 四键和 audio/UUID/anchor 计数一致；2 个 traceability tests 通过；生产目录无侵入式命中；diff 限于独立包、测试、benchmark、文档与配置 | 已实现 |

## 目标机验收边界

下列项仍需在实际 Ascend 310P、目标 Qwen3-ASR-1.7B 制品和经批准的验证清单上执行：精确目标运行时、32 哈希块初始化、逐 token/语言/文本等价、多语言 CER/WER、可观测 reset/LRU/Session 重建、完整性能矩阵、msprof 分段、真实 Encoder Cache 指标源和 NPU 内存。当前本地审计不能声明性能收益或 Encoder Cache 命中。

若 32 初始化或完整正确性失败，按 `docs/310p-validation.md` 的可执行 128 命令重跑全部 token 等价性、质量、性能、profiling 和内存流程；这只改变哈希粒度，不能绕过 PCM 内容身份或 Session/epoch 隔离。
